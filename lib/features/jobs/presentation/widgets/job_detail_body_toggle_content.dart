import 'package:flutter/material.dart';

class JobDetailBodyToggleContent extends StatelessWidget {
  final Widget content;
  const JobDetailBodyToggleContent({
    super.key,
    required this.content,
  });

  @override
  Widget build(BuildContext context) {
    return content;
  }
}
